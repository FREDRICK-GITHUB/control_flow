fn main() {
    for number in (1..4).rev(){ // loop starts from the first item in range and ending before the declared number in range
        println!("{}!",number);
    }
    println!("LIFTOFF!!!");
}
